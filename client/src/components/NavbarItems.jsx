import React from "react";

const NavbarItems = () => {
	return (
		<div className=" py-4 px-[80px] h-[5rem] -z-10 flex items-center justify-center fixed top-[5rem] w-screen">
			NavbarItems
		</div>
	);
};

export default NavbarItems;
